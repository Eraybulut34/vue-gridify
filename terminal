npm install -g npm
npm create vite vue-gridify --template vue-ts
cd vue-gridify
npm install
npm add -D vite-plugin-dts
npm add -D @types/node
npm dev

# NPM hesabına giriş yap
npm login

# Paketi derle
npm build

# Giriş yaptıktan sonra tekrar yayınlamayı dene
npm publish --access public

